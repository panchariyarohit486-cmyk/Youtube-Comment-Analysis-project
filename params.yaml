make_dataset: 
  test_size: 0.2
  random_state: 42

data_preprocessing:
  remove_stopwords: true
  keep_negations: ["not", "but", "however", "no", "yet"]
  lemmatize: true

build_features:
  max_features: 1000
  ngram_range: [1, 3]
  random_state: 42

